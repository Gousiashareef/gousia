<div class="remoteStreams" ng-controller="RemoteStreamsController">
	<h2>Remote Streams</h2>

	<div id="remoteVideosContainer"></div>

	<table>
    	<thead>
    		<tr>
        		<th>Stream</th><th>1-way</th><th>2-way</th>
   			</tr>
   		</thead>
    	<tbody>
    		<tr ng-repeat="stream in remoteStreams" ng-class="{'selected' : stream.isPlaying}">
        		<td><a>{{stream.name}}</a></td>
                <td ng-click="view(stream)"><a>View</a></td>
                <td ng-click="call(stream)"><a>Call</a></td>
    		</tr>
		</tbody>
	</table>

	<li ng-click="loadData()" style="width: 100px">
        <a href>Refresh</a>
    </li>
<div class="remoteStreams">
